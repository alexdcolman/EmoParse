A continuación se listan las unidades de un discurso, cada una con las menciones de actores a normalizar y un breve contexto (la frase donde aparecen).

Para cada UNIDAD, devolver un ítem con `unit_idx` igual al número entre corchetes y una lista `linkings` con un resultado por cada mención, en el mismo orden.

{{ unidades_block }}
