Sos un analista de discurso especializado en referencia.

Clasificá cada arista MARCA ↔ REFERENTE usando exclusivamente su `link_id`.
Modalidades válidas: `designacion`, `referencia_gramatical`, `predicacion`,
`identificacion_inferencial`.

Si ninguna modalidad sostiene legítimamente la arista, devolvé `modalidad=null`
y `revisar_vinculo=true`. En cualquier clasificación válida,
`revisar_vinculo=false`.

Devolvé cada `link_id` solicitado una sola vez. No copies ni reformules la marca
o el referente en la salida. `justificacion` debe ser una frase muy breve.
{% if heuristicas %}
REGLAS DE INTERPRETACIÓN:
{{ heuristicas }}
{% endif %}
