name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONUNBUFFERED: "1"

jobs:
  lint:
    name: Lint y formato
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Instalar herramientas de desarrollo
        run: python -m pip install -e ".[dev]"

      - name: Ruff lint
        run: ruff check src tests scripts

      - name: Ruff format
        run: ruff format --check src tests scripts

  types:
    name: Tipos estáticos
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Instalar dependencias de desarrollo
        run: python -m pip install -e ".[dev]"

      - name: Mypy
        run: mypy

  test:
    name: Contratos · Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Instalar proyecto y dependencias de pruebas
        run: python -m pip install -e ".[dev,scraping,ui]"

      - name: Contratos permanentes
        run: python -m pytest tests/contrato -q

      - name: Andamio informativo
        continue-on-error: true
        run: python -m pytest tests/andamio -q

      - name: Verificar colección
        run: python -m pytest --collect-only -q

      - name: Construir wheel
        run: python -m build --wheel

  docs:
    name: Documentación generada
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Instalar proyecto
        run: python -m pip install -e ".[dev]"

      - name: Verificar referencia del CLI
        run: python scripts/gen_cli_reference.py --check

  preflight-71b:
    name: Preflight temporal 7.1B
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Instalar proyecto y dependencias de pruebas
        run: python -m pip install -e ".[dev,scraping,ui]"

      - name: Focales 7.1B
        run: python -m pytest tests/contrato/test_cite_corpus.py -q

      - name: Suite sin LLM
        run: python -m pytest -m "not llm" -q

      - name: Generar referencia CLI exacta
        run: python scripts/gen_cli_reference.py

      - name: Diff check
        run: git diff --check

      - name: Empaquetar referencia generada
        uses: actions/upload-artifact@v4
        with:
          name: cli-reference-71b
          path: |
            docs/comandos.md
            docs/comandos.html
